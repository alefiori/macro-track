# Supabase — copy this file to .env and fill in your project's values.
# Settings → API in the Supabase dashboard.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# USDA FoodData Central API key (free): https://fdc.nal.usda.gov/api-key-signup.html
# Optional — falls back to the shared, heavily rate-limited DEMO_KEY if unset.
VITE_USDA_API_KEY=DEMO_KEY
